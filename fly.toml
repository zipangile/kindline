# fly.toml app configuration file generated for kindline-care on 2026-04-28T17:03:38Z
#
# See https://fly.io/docs/reference/configuration/ for information about how to use this file.
#

app = 'kindline-care'
primary_region = 'jnb'

[build]

[deploy]
  strategy = 'immediate'

[env]
  PORT = '3000'

[[mounts]]
  source = 'kindline_care_data'
  destination = '/data'

[http_service]
  internal_port = 3000
  force_https = true
  auto_stop_machines = 'stop'
  auto_start_machines = true
  min_machines_running = 0
  processes = ['app']

[[vm]]
  size = 'shared-cpu-1x'
  memory = '1gb'
